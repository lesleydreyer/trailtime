import React from 'react';
import './index.css';

const TrailCalendar = () => {
    return (
        <React.Fragment>
            <h3>Calendar</h3>
            {/*<input type="datetime-local" />*/}
            <p>Stay tuned. Eventually I would like to add the ability to schedule jeep meetups on the calendar!</p>
        </React.Fragment>
    );
}

export default TrailCalendar;